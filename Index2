<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Be My Valentine ❤️</title>

<style>

/* =====================
   GLOBAL STYLES
===================== */

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  height: 100vh;
  background: linear-gradient(270deg,#ff6a88,#ff99ac,#ff4b6e);
  background-size: 400% 400%;
  animation: bgFlow 12s ease infinite;
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: 'Segoe UI', sans-serif;
  overflow: hidden;
}

@keyframes bgFlow {
  0% {background-position:0% 50%;}
  50% {background-position:100% 50%;}
  100% {background-position:0% 50%;}
}

/* =====================
   FLOATING HEARTS
===================== */

.floating-heart {
  position: absolute;
  font-size: 18px;
  opacity: 0.8;
  animation: floatUp 8s linear infinite;
}

@keyframes floatUp {
  0% {
    transform: translateY(110vh) translateX(0);
    opacity: 1;
  }
  100% {
    transform: translateY(-10vh) translateX(40px);
    opacity: 0;
  }
}

/* =====================
   HEART CONTAINER
===================== */

.heart {
  position: relative;
  width: 300px;
  height: 270px;
  background: linear-gradient(145deg,#ff0033,#ff4d6d);
  transform: rotate(-45deg);
  animation: heartbeat 1.4s infinite;
  box-shadow: 0 0 30px rgba(255,0,80,0.7);
}

@keyframes heartbeat {
  0% {transform: rotate(-45deg) scale(1);}
  30% {transform: rotate(-45deg) scale(1.08);}
  60% {transform: rotate(-45deg) scale(1);}
  100% {transform: rotate(-45deg) scale(1);}
}

.heart::before,
.heart::after {
  content: "";
  position: absolute;
  width: 300px;
  height: 270px;
  background: inherit;
  border-radius: 50%;
}

.heart::before {
  top: -150px;
}

.heart::after {
  left: 150px;
}

/* =====================
   HEART CONTENT
===================== */

.content {
  position: absolute;
  width: 100%;
  height: 100%;
  transform: rotate(45deg);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  color: white;
  padding: 20px;
}

.content h1 {
  font-size: 22px;
  margin-bottom: 15px;
  animation: textFloat 2s ease;
}

@keyframes textFloat {
  from {opacity:0; transform: translateY(15px);}
  to {opacity:1; transform: translateY(0);}
}

/* =====================
   BUTTONS
===================== */

.buttons {
  margin-top: 10px;
}

button {
  padding: 11px 20px;
  margin: 6px;
  border: none;
  border-radius: 25px;
  font-size: 14px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.25s ease;
}

button:hover {
  transform: scale(1.12);
  box-shadow: 0 6px 15px rgba(0,0,0,0.25);
}

#yesBtn {
  background: linear-gradient(to right,#00ffb3,#00ffaa);
}

#noBtn {
  background: white;
}

#loopBtn {
  background: linear-gradient(to right,#ffe066,#ffd43b);
  display: none;
}

/* =====================
   RESPONSIVE
===================== */

@media (max-width: 500px) {

  .heart {
    width: 240px;
    height: 215px;
  }

  .heart::before,
  .heart::after {
    width: 240px;
    height: 215px;
  }

  .heart::before {
    top: -120px;
  }

  .heart::after {
    left: 120px;
  }

  .content h1 {
    font-size: 18px;
  }
}

</style>
</head>

<body>

<!-- HEART -->
<div class="heart">
  <div class="content">
    <h1>Will you be my Valentine? 💖</h1>

    <div class="buttons">
      <button id="yesBtn">Yes ❤️</button>
      <button id="noBtn">No 💔</button>
      <button id="loopBtn"></button>
    </div>
  </div>
</div>

<!-- CONFETTI -->
<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

<script>

/* FLOATING HEART GENERATOR */

function createFloatingHeart() {
  const heart = document.createElement("div");
  heart.className = "floating-heart";
  heart.innerHTML = "❤️";
  heart.style.left = Math.random() * 100 + "vw";
  heart.style.fontSize = Math.random() * 22 + 14 + "px";
  document.body.appendChild(heart);

  setTimeout(() => heart.remove(), 8000);
}

setInterval(createFloatingHeart, 400);

/* BUTTON LOOP LOGIC */

const noBtn = document.getElementById("noBtn");
const loopBtn = document.getElementById("loopBtn");
const yesBtn = document.getElementById("yesBtn");

const loopTexts = [
  "Are you sure?",
  "This is not possible 😢",
  "Think again 🤔"
];

let step = 0;

noBtn.addEventListener("click", () => {
  noBtn.style.display = "none";
  loopBtn.style.display = "inline-block";
  loopBtn.textContent = loopTexts[step];
});

loopBtn.addEventListener("click", () => {
  step++;

  if (step >= loopTexts.length) {
    step = 0;
    loopBtn.style.display = "none";
    noBtn.style.display = "inline-block";
  } else {
    loopBtn.textContent = loopTexts[step];
  }
});

/* YES BUTTON */

yesBtn.addEventListener("click", () => {

  confetti({
    particleCount: 220,
    spread: 120,
    origin: { y: 0.6 }
  });

  setTimeout(() => {
    alert("Congratulations ❤️\nI knew you would say YES!");
  }, 450);
});

</script>

</body>
</html>
